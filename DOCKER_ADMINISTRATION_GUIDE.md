# Руководство по администрированию Docker, Prometheus, Grafana и Jenkins

## 📋 Содержание
1. [Docker и Dockerfile](#docker-и-dockerfile)
2. [Docker Compose](#docker-compose)
3. [Prometheus](#prometheus)
4. [Grafana](#grafana)
5. [Jenkins](#jenkins)
6. [Вопросы и ответы для зачета](#вопросы-и-ответы-для-зачета)

---

## 🐳 Docker и Dockerfile

### Что такое Docker?
**Docker** — это платформа для контейнеризации приложений, которая позволяет упаковать приложение со всеми его зависимостями в изолированный контейнер.

### Что такое Dockerfile?
**Dockerfile** — это текстовый файл с инструкциями для создания Docker-образа. Каждая строка — это команда, которая выполняется при сборке образа.

### Структура Dockerfile в проекте

#### Пример: `demo-rest/Dockerfile`

```dockerfile
# Базовый образ - JRE 21 на Alpine Linux (легковесный)
FROM eclipse-temurin:21-jre-alpine

# Рабочая директория внутри контейнера
WORKDIR /app

# Создание непривилегированного пользователя (безопасность)
RUN addgroup -S javauser && adduser -S javauser -G javauser

# Копирование JAR-файла в образ
ARG JAR_FILE=target/*.jar
COPY ${JAR_FILE} app.jar

# Изменение владельца файла
RUN chown javauser:javauser /app/app.jar

# Переключение на непривилегированного пользователя
USER javauser

# Документирование порта (не открывает порт, только информация)
EXPOSE 8080

# Настройки JVM для контейнера
ENV JAVA_OPTS="-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0"

# Health check - проверка работоспособности
HEALTHCHECK --interval=30s --timeout=3s --start-period=60s --retries=3 \
    CMD wget -q --spider http://localhost:8080/actuator/health || exit 1

# Команда запуска приложения
ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]
```

### Объяснение команд Dockerfile:

1. **FROM** — базовый образ (OS + runtime)
   - `eclipse-temurin:21-jre-alpine` — OpenJDK 21 JRE на Alpine Linux (маленький размер)

2. **WORKDIR** — устанавливает рабочую директорию для последующих команд

3. **RUN** — выполняет команду во время сборки образа
   - `addgroup -S javauser` — создает системную группу
   - `adduser -S javauser -G javauser` — создает системного пользователя

4. **ARG** — переменная времени сборки
   - `JAR_FILE=target/*.jar` — путь к JAR-файлу

5. **COPY** — копирует файлы из хоста в образ
   - `${JAR_FILE} app.jar` — копирует JAR в образ как `app.jar`

6. **USER** — переключает пользователя (безопасность: не запускаем от root)

7. **EXPOSE** — документирует порт (не открывает его автоматически)

8. **ENV** — устанавливает переменные окружения
   - `JAVA_OPTS` — параметры JVM для контейнера

9. **HEALTHCHECK** — проверка здоровья контейнера
   - `interval=30s` — проверка каждые 30 секунд
   - `timeout=3s` — таймаут запроса
   - `start-period=60s` — период запуска (первые 60 сек не проверяем)
   - `retries=3` — количество попыток перед пометкой как unhealthy

10. **ENTRYPOINT** — команда, которая выполняется при запуске контейнера

### Почему используется Alpine Linux?
- **Маленький размер** (5-10 MB vs 100+ MB для обычного Linux)
- **Быстрая загрузка** образов
- **Меньше уязвимостей** (меньше пакетов = меньше поверхность атаки)

### Почему создается отдельный пользователь?
- **Безопасность**: приложение не работает от root
- **Принцип наименьших привилегий**: пользователь имеет только необходимые права
- **Изоляция**: если контейнер скомпрометирован, ущерб ограничен

---

## 🐙 Docker Compose

### Что такое Docker Compose?
**Docker Compose** — инструмент для определения и запуска многоконтейнерных приложений. Использует YAML-файл для конфигурации.

### Структура `docker-compose.yml`

```yaml
services:
  # Определение сервисов
  service-name:
    image: image-name
    container_name: container-name
    ports:
      - "host:container"
    networks:
      - network-name
    volumes:
      - volume-name:/path
    environment:
      - KEY=value
    depends_on:
      - other-service
```

### Компоненты docker-compose.yml в проекте:

#### 1. **Инфраструктура**

##### RabbitMQ (брокер сообщений)
```yaml
rabbitmq:
  image: rabbitmq:3.13-management
  container_name: rabbitmq
  ports:
    - "5672:5672"    # AMQP порт
    - "15672:15672"  # Web UI порт
  networks:
    - microservices-net
  healthcheck:
    test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
    interval: 10s
    timeout: 5s
    retries: 5
  volumes:
    - rabbitmq-data:/var/lib/rabbitmq
```

**Объяснение:**
- `ports: "5672:5672"` — маппинг портов (хост:контейнер)
- `healthcheck` — проверка работоспособности
- `volumes` — постоянное хранилище данных

##### Zipkin (трассировка)
```yaml
zipkin:
  image: openzipkin/zipkin
  container_name: zipkin
  ports:
    - "9411:9411"
  networks:
    - microservices-net
```

#### 2. **Мониторинг**

##### Prometheus
```yaml
prometheus:
  image: prom/prometheus:latest
  container_name: prometheus
  volumes:
    - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    - prometheus-data:/prometheus
  command:
    - '--config.file=/etc/prometheus/prometheus.yml'
    - '--storage.tsdb.path=/prometheus'
  ports:
    - "9090:9090"
  networks:
    - microservices-net
```

**Объяснение:**
- `:ro` — read-only монтирование (конфиг только для чтения)
- `command` — переопределение команды запуска
- `volumes` — данные сохраняются между перезапусками

##### Grafana
```yaml
grafana:
  image: grafana/grafana:latest
  container_name: grafana
  ports:
    - "3000:3000"
  environment:
    - GF_SECURITY_ADMIN_USER=admin
    - GF_SECURITY_ADMIN_PASSWORD=admin
  volumes:
    - grafana-data:/var/lib/grafana
    - ./grafana/provisioning:/etc/grafana/provisioning:ro
  depends_on:
    - prometheus
```

**Объяснение:**
- `environment` — переменные окружения
- `depends_on` — зависимость: Grafana запустится после Prometheus
- `provisioning` — автоматическая настройка дашбордов и источников данных

#### 3. **CI/CD**

##### Jenkins
```yaml
jenkins:
  image: jenkins/jenkins:lts-jdk21
  container_name: jenkins
  privileged: true
  user: root
  ports:
    - "8085:8080"
  volumes:
    - jenkins-data:/var/jenkins_home
    - //var/run/docker.sock:/var/run/docker.sock
    - ./:/workspace:ro
  environment:
    - JAVA_OPTS=-Djenkins.install.runSetupWizard=false
```

**Объяснение:**
- `privileged: true` — расширенные привилегии (для доступа к Docker)
- `//var/run/docker.sock` — доступ к Docker daemon хоста (Windows путь)
- `:ro` — read-only доступ к workspace

#### 4. **Микросервисы**

##### Пример: demo-rest
```yaml
demo-rest:
  build:
    context: ./demo-rest
    dockerfile: Dockerfile
  container_name: demo-rest
  ports:
    - "8080:8080"
  environment:
    - SPRING_RABBITMQ_HOST=rabbitmq
    - GRPC_CLIENT_ANALYTICS_SERVICE_ADDRESS=static://analytics-service:9090
  depends_on:
    rabbitmq:
      condition: service_healthy
    analytics-service:
      condition: service_started
  networks:
    - microservices-net
```

**Объяснение:**
- `build` — сборка образа из Dockerfile
- `context` — директория с Dockerfile
- `depends_on` с `condition` — ожидание готовности зависимого сервиса
- `networks` — подключение к сети для связи между сервисами

### Сети (Networks)

```yaml
networks:
  microservices-net:
    driver: bridge
```

**Объяснение:**
- `bridge` — изолированная сеть для контейнеров
- Контейнеры могут обращаться друг к другу по имени сервиса
- Изоляция от других Docker-сетей

### Volumes (Тома)

```yaml
volumes:
  rabbitmq-data:
  prometheus-data:
  grafana-data:
  jenkins-data:
```

**Объяснение:**
- **Named volumes** — именованные тома для постоянного хранения
- Данные сохраняются между перезапусками контейнеров
- Управляются Docker'ом

### Команды Docker Compose:

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Пересборка и запуск
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f service-name

# Остановка с удалением volumes
docker-compose down -v
```

---

## 📊 Prometheus

### Что такое Prometheus?
**Prometheus** — система мониторинга и сбора метрик с временными рядами. Собирает метрики через HTTP-запросы (pull-модель).

### Конфигурация `prometheus.yml`

```yaml
global:
  scrape_interval: 5s      # Интервал сбора метрик
  evaluation_interval: 5s  # Интервал оценки правил

scrape_configs:
  # Самомониторинг Prometheus
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Микросервисы Spring Boot
  - job_name: "demo_app"
    metrics_path: "/actuator/prometheus"
    scrape_interval: 5s
    static_configs:
      - targets:
          - "demo-rest:8080"
          - "notification-service:8083"
          - "audit-service:8082"
          - "analytics-service:8081"
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
        regex: "([^:]+):.*"
        replacement: "$1"
```

### Объяснение конфигурации:

1. **global** — глобальные настройки
   - `scrape_interval` — как часто собирать метрики
   - `evaluation_interval` — как часто проверять правила алертов

2. **scrape_configs** — конфигурация сбора метрик
   - `job_name` — имя задачи сбора
   - `metrics_path` — путь к эндпоинту метрик
   - `static_configs` — статический список целей
   - `targets` — адреса сервисов (используются имена из docker-compose)

3. **relabel_configs** — переименование меток
   - Извлекает имя хоста из адреса для метки `instance`

### Как Prometheus собирает метрики?

1. **Pull-модель**: Prometheus сам запрашивает метрики у сервисов
2. **HTTP-запрос**: `GET http://demo-rest:8080/actuator/prometheus`
3. **Формат**: Prometheus text format
4. **Хранение**: Time Series Database (TSDB)

### Spring Boot Actuator

Для экспорта метрик в Spring Boot используется **Actuator**:

```properties
# application.properties
management.endpoints.web.exposure.include=health,info,prometheus
management.metrics.export.prometheus.enabled=true
```

Эндпоинт: `http://localhost:8080/actuator/prometheus`

### Типы метрик:

- **Counter** — счетчик (только увеличивается)
- **Gauge** — значение (может увеличиваться/уменьшаться)
- **Histogram** — распределение значений
- **Summary** — квантили

### Зачем нужен Prometheus в микросервисной архитектуре?

#### Проблемы без мониторинга:

1. **"Слепота" в production**
   - Не знаете, что происходит с приложением
   - Не видите, какой сервис тормозит
   - Не понимаете, почему пользователи жалуются

2. **Реакция на проблемы вместо профилактики**
   - Узнаете о проблеме только когда пользователи жалуются
   - Не можете предсказать проблемы до их возникновения
   - Тратите время на поиск причины вместо исправления

3. **Невозможность масштабирования**
   - Не знаете, какой сервис нагружен больше всего
   - Не можете принять решение о масштабировании
   - Тратите ресурсы впустую или не хватает мощности

#### Что дает Prometheus:

1. **Сбор метрик в реальном времени**
   - CPU, память, количество запросов
   - Время ответа, количество ошибок
   - Размер очередей, использование БД

2. **Исторические данные**
   - Видите тренды за дни/недели/месяцы
   - Можете сравнить "сегодня" с "вчера"
   - Анализируете пики нагрузки

3. **Автоматические алерты**
   - Уведомления при превышении порогов
   - Предупреждения до того, как пользователи заметят проблему
   - Интеграция с Slack, Email, PagerDuty

#### Примеры метрик в вашем проекте:

```promql
# Количество HTTP-запросов в секунду
rate(http_server_requests_seconds_count[5m])

# Среднее время ответа
rate(http_server_requests_seconds_sum[5m]) / rate(http_server_requests_seconds_count[5m])

# Использование памяти JVM
jvm_memory_used_bytes{area="heap"}

# Количество активных подключений к RabbitMQ
rabbitmq_connections

# Количество ошибок 5xx
rate(http_server_requests_seconds_count{status=~"5.."}[5m])
```

#### Реальные сценарии использования:

**Сценарий 1: Производительность**
- **Проблема**: Пользователи жалуются на медленную работу
- **С Prometheus**: Видите график времени ответа, находите медленный эндпоинт
- **Решение**: Оптимизируете конкретный метод или добавляете кэширование

**Сценарий 2: Утечка памяти**
- **Проблема**: Сервис падает каждые несколько часов
- **С Prometheus**: Видите рост использования памяти на графике
- **Решение**: Находите утечку памяти в коде до того, как сервис упадет

**Сценарий 3: Масштабирование**
- **Проблема**: Не знаете, нужно ли масштабировать
- **С Prometheus**: Видите загрузку CPU/памяти каждого сервиса
- **Решение**: Масштабируете только те сервисы, которые действительно нагружены

**Сценарий 4: Анализ нагрузки**
- **Проблема**: Не понимаете, когда пиковая нагрузка
- **С Prometheus**: Видите график запросов по времени
- **Решение**: Планируете масштабирование заранее на пиковые часы

---

## 📈 Grafana

### Что такое Grafana?
**Grafana** — платформа для визуализации и анализа метрик. Создает графики, дашборды и алерты.

### Конфигурация Grafana

#### 1. Datasource (Источник данных) — `grafana/provisioning/datasources/datasources.yml`

```yaml
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "5s"
      httpMethod: POST
```

**Объяснение:**
- `type: prometheus` — тип источника данных
- `url: http://prometheus:9090` — адрес Prometheus (имя сервиса из docker-compose)
- `isDefault: true` — источник по умолчанию
- `editable: false` — нельзя редактировать через UI (только через конфиг)

#### 2. Dashboards Provisioning — `grafana/provisioning/dashboards/dashboards.yml`

```yaml
apiVersion: 1

providers:
  - name: 'Microservices Dashboards'
    orgId: 1
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
```

**Объяснение:**
- `type: file` — загрузка дашбордов из файлов
- `path` — путь к JSON-файлам дашбордов
- `updateIntervalSeconds` — как часто проверять обновления

#### 3. Environment Variables

```yaml
environment:
  - GF_SECURITY_ADMIN_USER=admin
  - GF_SECURITY_ADMIN_PASSWORD=admin
  - GF_USERS_ALLOW_SIGN_UP=false
  - GF_SERVER_DOMAIN=localhost
```

**Объяснение:**
- `GF_SECURITY_ADMIN_USER` — имя администратора
- `GF_SECURITY_ADMIN_PASSWORD` — пароль администратора
- `GF_USERS_ALLOW_SIGN_UP` — запрет регистрации новых пользователей

### Что такое Provisioning?
**Provisioning** — автоматическая настройка Grafana при запуске:
- Источники данных создаются автоматически
- Дашборды загружаются автоматически
- Не нужно настраивать вручную через UI

### Структура дашборда

Дашборд — это JSON-файл с описанием:
- Панелей (panels) — графиков
- Запросов (queries) — PromQL запросы
- Переменных (variables) — параметры фильтрации

### Зачем нужна Grafana, если есть Prometheus?

#### Проблема: Prometheus — это только данные

Prometheus хранит метрики, но:
- ❌ Нет удобного интерфейса для просмотра
- ❌ Нет красивых графиков
- ❌ Сложно анализировать данные
- ❌ Нет дашбордов для команды

#### Решение: Grafana — визуализация

Grafana превращает сырые данные Prometheus в:
- ✅ Красивые графики и дашборды
- ✅ Интуитивный веб-интерфейс
- ✅ Возможность сравнения периодов
- ✅ Алерты и уведомления

#### Практические преимущества Grafana:

**1. Визуализация для команды**
- Разработчики видят метрики своего сервиса
- DevOps видит общую картину системы
- Менеджмент видит бизнес-метрики
- Все говорят на одном языке (графики)

**2. Быстрая диагностика проблем**
- Открыли дашборд → сразу видно проблему
- Не нужно писать PromQL запросы каждый раз
- Предустановленные дашборды для типовых задач
- Сравнение "до" и "после" на одном экране

**3. Исторический анализ**
- Видите, что было неделю назад
- Сравниваете производительность версий
- Анализируете тренды нагрузки
- Планируете масштабирование на основе данных

**4. Алерты и уведомления**
- Автоматические уведомления при проблемах
- Интеграция с Slack, Email, Telegram
- Настраиваемые пороги для разных сервисов
- Эскалация алертов (критичные → важные → информационные)

#### Примеры дашбордов в вашем проекте:

**Дашборд "Обзор микросервисов":**
- График запросов по всем сервисам
- Использование памяти/CPU
- Количество ошибок
- Время ответа

**Дашборд "RabbitMQ":**
- Размер очередей
- Скорость обработки сообщений
- Количество подключений
- Использование памяти

**Дашборд "JVM метрики":**
- Heap память
- Garbage Collection
- Threads
- Class loading

#### Почему Prometheus + Grafana вместе?

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│ Микросервисы │ ──────> │ Prometheus │ ──────> │   Grafana   │
│  (метрики)   │  pull   │  (хранение) │  query  │(визуализация)│
└─────────────┘         └─────────────┘         └─────────────┘
```

- **Prometheus** — собирает и хранит метрики (backend)
- **Grafana** — показывает метрики красиво (frontend)
- Вместе они дают полную картину мониторинга

#### Реальные сценарии использования:

**Сценарий 1: Утренний standup**
- Открываете дашборд → видите все метрики за ночь
- За 30 секунд понимаете, были ли проблемы
- Не нужно копаться в логах

**Сценарий 2: Инцидент в production**
- Получили алерт → открыли дашборд
- Сразу видно, какой сервис упал
- Видите график до падения → понимаете причину
- Исправили → видите восстановление на графике

**Сценарий 3: Презентация для руководства**
- Показываете дашборд с бизнес-метриками
- Видно рост пользователей, производительность
- Данные говорят сами за себя

**Сценарий 4: Оптимизация производительности**
- Видите медленный эндпоинт на графике
- Оптимизируете код
- Сравниваете "до" и "после" на одном дашборде
- Видите улучшение в реальном времени

---

## 🔧 Jenkins

### Что такое Jenkins?
**Jenkins** — сервер автоматизации для CI/CD (Continuous Integration / Continuous Deployment). Автоматизирует сборку, тестирование и развертывание.

### Конфигурация Jenkins в docker-compose.yml

```yaml
jenkins:
  image: jenkins/jenkins:lts-jdk21
  container_name: jenkins
  privileged: true
  user: root
  ports:
    - "8085:8080"
    - "50000:50000"
  volumes:
    - jenkins-data:/var/jenkins_home
    - //var/run/docker.sock:/var/run/docker.sock
    - ./:/workspace:ro
    - ./jenkins/casc.yaml:/var/jenkins_home/casc.yaml:ro
  environment:
    - JAVA_OPTS=-Djenkins.install.runSetupWizard=false
    - CASC_JENKINS_CONFIG=/var/jenkins_home/casc.yaml
```

### Объяснение параметров:

1. **privileged: true** — расширенные привилегии (нужно для доступа к Docker)

2. **user: root** — запуск от root (для доступа к Docker socket)

3. **volumes:**
   - `jenkins-data:/var/jenkins_home` — данные Jenkins (настройки, плагины, jobs)
   - `//var/run/docker.sock:/var/run/docker.sock` — доступ к Docker daemon хоста
   - `./:/workspace:ro` — монтирование проекта (read-only)
   - `casc.yaml` — конфигурация через код (Configuration as Code)

4. **environment:**
   - `JAVA_OPTS=-Djenkins.install.runSetupWizard=false` — отключение мастера настройки
   - `CASC_JENKINS_CONFIG` — путь к конфигурации

### Jenkinsfile (Pipeline as Code)

**Jenkinsfile** — определение pipeline в коде:

```groovy
pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                sh './mvnw clean package'
            }
        }
        stage('Test') {
            steps {
                sh './mvnw test'
            }
        }
        stage('Deploy') {
            steps {
                sh 'docker-compose up -d'
            }
        }
    }
}
```

### Преимущества Jenkins в Docker:

- **Изоляция**: Jenkins не влияет на хост-систему
- **Портативность**: легко перенести на другой сервер
- **Воспроизводимость**: одинаковое окружение везде
- **Масштабируемость**: можно запустить несколько экземпляров

### Docker-in-Docker (DinD)

Для сборки Docker-образов в Jenkins нужен доступ к Docker daemon:
- Монтирование `/var/run/docker.sock` — использование Docker хоста
- Или запуск Docker внутри контейнера (DinD)

---

## ❓ Вопросы и ответы для зачета

### Раздел 1: Docker и Dockerfile

**В1: Что такое Docker и зачем он нужен?**
**О1:** Docker — платформа для контейнеризации приложений. Позволяет упаковать приложение со всеми зависимостями в изолированный контейнер. Преимущества: изоляция, портативность, масштабируемость, одинаковое окружение на всех машинах.

**В2: Что такое Dockerfile и какие основные команды в нем используются?**
**О2:** Dockerfile — файл с инструкциями для создания образа. Основные команды:
- `FROM` — базовый образ
- `WORKDIR` — рабочая директория
- `RUN` — выполнение команды при сборке
- `COPY` — копирование файлов
- `USER` — переключение пользователя
- `EXPOSE` — документирование порта
- `ENV` — переменные окружения
- `HEALTHCHECK` — проверка здоровья
- `ENTRYPOINT` / `CMD` — команда запуска

**В3: Почему в Dockerfile создается отдельный пользователь вместо root?**
**О3:** Для безопасности. Принцип наименьших привилегий: приложение не должно работать от root. Если контейнер скомпрометирован, ущерб ограничен правами непривилегированного пользователя.

**В4: Что такое multi-stage build и почему он используется?**
**О4:** Multi-stage build — использование нескольких этапов сборки в одном Dockerfile. Позволяет уменьшить размер финального образа, исключив инструменты сборки (Maven, компиляторы) из runtime-образа.

**В5: Что такое HEALTHCHECK и зачем он нужен?**
**О5:** HEALTHCHECK — автоматическая проверка работоспособности контейнера. Docker периодически выполняет команду и помечает контейнер как healthy/unhealthy. Используется для автоматического перезапуска и балансировки нагрузки.

---

### Раздел 2: Docker Compose

**В6: Что такое Docker Compose и чем он отличается от Docker?**
**О6:** Docker Compose — инструмент для оркестрации многоконтейнерных приложений. Docker работает с одним контейнером, Compose управляет несколькими сервисами, их сетями, volumes и зависимостями через YAML-файл.

**В7: Что такое networks в Docker Compose и зачем они нужны?**
**О7:** Networks — изолированные сети для контейнеров. Позволяют контейнерам общаться друг с другом по имени сервиса, изолируют приложение от других Docker-сетей, обеспечивают безопасность.

**В8: Что такое volumes и какие типы volumes существуют?**
**О8:** Volumes — механизм постоянного хранения данных. Типы:
- **Named volumes** — именованные тома (управляются Docker)
- **Bind mounts** — монтирование директории хоста
- **tmpfs mounts** — временное хранилище в RAM

**В9: Что означает `depends_on` в docker-compose.yml?**
**О9:** `depends_on` — зависимость между сервисами. Указывает порядок запуска: сервис запустится только после запуска зависимых. `condition: service_healthy` — ожидание прохождения health check.

**В10: Как работает маппинг портов `"8080:8080"`?**
**О10:** Формат `"host:container`. Первое число — порт на хосте, второе — порт в контейнере. `"8080:8080"` означает: трафик на порт 8080 хоста перенаправляется на порт 8080 контейнера.

---

### Раздел 3: Prometheus

**В11: Что такое Prometheus и как он собирает метрики?**
**О11:** Prometheus — система мониторинга с временными рядами. Использует **pull-модель**: Prometheus сам запрашивает метрики у сервисов через HTTP (эндпоинт `/actuator/prometheus`). Хранит данные в TSDB.

**В12: Что такое scrape_interval и evaluation_interval?**
**О12:** 
- `scrape_interval` — интервал сбора метрик у сервисов (как часто запрашивать)
- `evaluation_interval` — интервал оценки правил алертов (как часто проверять условия)

**В13: Что такое job_name в конфигурации Prometheus?**
**О13:** `job_name` — имя задачи сбора метрик. Группирует несколько целей (targets) для сбора. Метрики помечаются меткой `job=job_name` для фильтрации в запросах.

**В14: Как Spring Boot экспортирует метрики для Prometheus?**
**О14:** Через **Spring Boot Actuator** с включенным Prometheus exporter. Эндпоинт: `/actuator/prometheus`. В `application.properties`: `management.endpoints.web.exposure.include=prometheus`.

**В15: Что такое relabel_configs?**
**О15:** `relabel_configs` — переименование и преобразование меток метрик. Позволяет изменить имена меток, добавить новые, удалить ненужные перед сохранением в TSDB.

**В15.1: Зачем нужен Prometheus в микросервисной архитектуре?**
**О15.1:** Prometheus решает проблемы "слепоты" в production:
1. **Сбор метрик в реальном времени** — CPU, память, запросы, ошибки
2. **Исторические данные** — видите тренды и можете сравнивать периоды
3. **Автоматические алерты** — уведомления до того, как пользователи заметят проблему
4. **Анализ производительности** — находите узкие места и оптимизируете
5. **Масштабирование** — принимаете решения на основе данных, а не догадок

Без Prometheus вы узнаете о проблемах только когда пользователи жалуются. С Prometheus вы видите проблемы заранее и можете их предотвратить.

---

### Раздел 4: Grafana

**В16: Что такое Grafana и для чего она используется?**
**О16:** Grafana — платформа для визуализации метрик. Создает графики, дашборды, алерты. Подключается к Prometheus как источнику данных и отображает метрики в удобном виде.

**В17: Что такое provisioning в Grafana?**
**О17:** Provisioning — автоматическая настройка Grafana при запуске через конфигурационные файлы. Автоматически создает datasources и загружает дашборды, не требуя ручной настройки через UI.

**В18: Как Grafana подключается к Prometheus?**
**О18:** Через datasource конфигурацию. В `datasources.yml` указывается:
- `type: prometheus`
- `url: http://prometheus:9090` (имя сервиса из docker-compose)
- Grafana использует PromQL для запросов метрик

**В19: Что такое дашборд в Grafana?**
**О19:** Дашборд — набор панелей (графиков, таблиц) для визуализации метрик. Состоит из:
- Панелей (panels) — визуализации
- Запросов (queries) — PromQL запросы
- Переменных (variables) — параметры фильтрации

**В20: Какие переменные окружения используются для настройки Grafana?**
**О20:** 
- `GF_SECURITY_ADMIN_USER` — имя администратора
- `GF_SECURITY_ADMIN_PASSWORD` — пароль администратора
- `GF_USERS_ALLOW_SIGN_UP` — разрешение регистрации
- `GF_SERVER_DOMAIN` — домен сервера

**В20.1: Зачем нужна Grafana, если есть Prometheus?**
**О20.1:** Prometheus хранит данные, но не имеет удобного интерфейса. Grafana решает это:
1. **Визуализация** — красивые графики вместо сырых данных
2. **Дашборды** — предустановленные панели для типовых задач
3. **Быстрая диагностика** — открыли дашборд → сразу видно проблему
4. **Исторический анализ** — сравнение периодов на одном экране
5. **Алерты** — уведомления в Slack/Email при проблемах
6. **Доступность для команды** — не нужно знать PromQL, чтобы видеть метрики

Prometheus = backend (хранение), Grafana = frontend (визуализация). Вместе они дают полную систему мониторинга.

---

### Раздел 5: Jenkins

**В21: Что такое Jenkins и для чего он используется?**
**О21:** Jenkins — сервер автоматизации для CI/CD. Автоматизирует сборку, тестирование, развертывание приложений. Поддерживает pipeline as code через Jenkinsfile.

**В22: Как Jenkins получает доступ к Docker для сборки образов?**
**О22:** Через монтирование Docker socket: `//var/run/docker.sock:/var/run/docker.sock`. Это позволяет Jenkins использовать Docker daemon хоста для сборки образов и запуска контейнеров.

**В23: Что такое Jenkinsfile и какие преимущества он дает?**
**О23:** Jenkinsfile — определение pipeline в коде (Groovy). Преимущества:
- Версионирование pipeline вместе с кодом
- Воспроизводимость
- Code review для изменений pipeline
- Pipeline as Code

**В24: Что означает `privileged: true` в конфигурации Jenkins?**
**О24:** `privileged: true` — расширенные привилегии контейнера. Необходимо для доступа к Docker daemon хоста. Дает контейнеру почти все возможности хоста (использовать с осторожностью).

**В25: Что такое Configuration as Code (CASC) в Jenkins?**
**О25:** CASC — управление конфигурацией Jenkins через YAML-файлы. Позволяет версионировать настройки Jenkins, автоматически настраивать плагины, пользователей, jobs при запуске.

---

### Раздел 6: Общие вопросы

**В26: В чем разница между `EXPOSE` и `ports` в Docker?**
**О26:** 
- `EXPOSE` в Dockerfile — только документирование порта (не открывает его)
- `ports` в docker-compose — фактическое пробрасывание порта на хост

**В27: Что такое health check и зачем он нужен?**
**О27:** Health check — проверка работоспособности контейнера. Docker периодически выполняет команду и помечает контейнер как healthy/unhealthy. Используется для:
- Автоматического перезапуска
- Балансировки нагрузки
- Зависимостей между сервисами (`depends_on` с `condition: service_healthy`)

**В28: В чем разница между `ENTRYPOINT` и `CMD`?**
**О28:**
- `ENTRYPOINT` — команда, которая всегда выполняется (не переопределяется)
- `CMD` — аргументы по умолчанию (можно переопределить)
- Обычно: `ENTRYPOINT ["java", "-jar"]` + `CMD ["app.jar"]`

**В29: Что такое Alpine Linux и почему он используется в Docker?**
**О29:** Alpine Linux — минималистичный Linux-дистрибутив (5-10 MB). Преимущества:
- Маленький размер образов
- Быстрая загрузка
- Меньше уязвимостей (меньше пакетов)
- Подходит для production

**В30: Как обеспечить безопасность Docker-контейнеров?**
**О30:**
1. Запуск от непривилегированного пользователя (`USER`)
2. Минимальный базовый образ (Alpine)
3. Ограничение ресурсов (CPU, память)
4. Read-only файловая система где возможно
5. Сканирование образов на уязвимости
6. Не использовать `privileged: true` без необходимости
7. Ограничение сетевого доступа

---

## 📚 Дополнительные материалы

### Полезные команды Docker:

```bash
# Сборка образа
docker build -t image-name:tag .

# Запуск контейнера
docker run -d -p 8080:8080 image-name

# Просмотр логов
docker logs -f container-name

# Вход в контейнер
docker exec -it container-name sh

# Остановка и удаление
docker stop container-name
docker rm container-name

# Очистка неиспользуемых ресурсов
docker system prune -a
```

### Полезные команды Docker Compose:

```bash
# Запуск в фоне
docker-compose up -d

# Остановка
docker-compose down

# Пересборка
docker-compose up -d --build

# Логи
docker-compose logs -f service-name

# Масштабирование
docker-compose up -d --scale service-name=3
```

---

## 🎯 Итоговая схема архитектуры

```
┌─────────────────────────────────────────────────────────┐
│                    Docker Compose                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  RabbitMQ    │  │   Zipkin     │  │  Prometheus │  │
│  │  (5672)      │  │   (9411)     │  │   (9090)    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Grafana    │  │   Jenkins    │  │ demo-rest   │  │
│  │   (3000)     │  │   (8085)     │  │   (8080)    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │audit-service │  │analytics-svc │  │notification  │  │
│  │   (8082)     │  │   (8081)     │  │   (8083)    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                          │
│              microservices-net (bridge)                 │
└─────────────────────────────────────────────────────────┘
```

**Поток данных:**
1. Микросервисы → RabbitMQ (сообщения)
2. Микросервисы → Zipkin (трассировка)
3. Микросервисы → Prometheus (метрики через Actuator)
4. Prometheus → Grafana (визуализация)
5. Jenkins → Docker (сборка и деплой)

---

**Удачи на зачете! 🎓**

