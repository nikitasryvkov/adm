# Microservices Docker Deployment

Этот документ описывает запуск проекта микросервисов с использованием Docker, включая мониторинг (Prometheus, Grafana) и CI/CD (Jenkins).

## 🏗️ Архитектура

```
┌──────────────────────────────────────────────────────────────────┐
│                      INFRASTRUCTURE                               │
├──────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │  RabbitMQ   │  │   Zipkin    │  │  Prometheus │              │
│  │   :5672     │  │   :9411     │  │    :9090    │              │
│  │   :15672    │  │             │  │             │              │
│  └─────────────┘  └─────────────┘  └──────┬──────┘              │
│                                           │                      │
│  ┌─────────────┐  ┌─────────────┐         │                      │
│  │   Grafana   │◄─┤  Scrapes    │─────────┘                      │
│  │    :3000    │  │   metrics   │                                │
│  └─────────────┘  └─────────────┘                                │
├──────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐                                                 │
│  │   Jenkins   │ CI/CD Pipeline                                  │
│  │    :8085    │                                                 │
│  └─────────────┘                                                 │
├──────────────────────────────────────────────────────────────────┤
│                       MICROSERVICES                               │
├──────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │  Demo REST  │  │  Analytics  │  │    Audit    │              │
│  │    :8080    │  │    :8081    │  │    :8082    │              │
│  │   (main)    │  │   (gRPC)    │  │  (events)   │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
│                                                                   │
│  ┌─────────────┐                                                 │
│  │ Notification│                                                 │
│  │    :8083    │                                                 │
│  │(WebSocket)  │                                                 │
│  └─────────────┘                                                 │
└──────────────────────────────────────────────────────────────────┘
```

## 🚀 Быстрый старт

### Вариант 1: Полная сборка с нуля

```bash
# 1. Подготовка библиотек (сборка контрактов)
prepare-libs.bat

# 2. Сборка и запуск
build.bat
start.bat
```

### Вариант 2: Если JAR уже собраны

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка
docker-compose down
```

## 📦 Порты сервисов

| Сервис | Порт | Описание |
|--------|------|----------|
| Demo REST API | 8080 | Основной REST/GraphQL API |
| Analytics Service | 8081 (HTTP), 9091 (gRPC) | gRPC сервис аналитики |
| Audit Service | 8082 | Сервис аудита событий |
| Notification Service | 8083 | WebSocket уведомления |
| RabbitMQ | 5672, 15672 | Message broker (Management UI) |
| Zipkin | 9411 | Distributed tracing |
| Prometheus | 9090 | Сбор метрик |
| Grafana | 3000 | Визуализация метрик |
| Jenkins | 8085 | CI/CD |

## 🔗 Ссылки после запуска

- **REST API**: http://localhost:8080
- **Swagger UI**: http://localhost:8080/swagger-ui.html
- **GraphiQL**: http://localhost:8080/graphiql
- **WebSocket Demo**: http://localhost:8083
- **RabbitMQ Console**: http://localhost:15672 (guest/guest)
- **Zipkin**: http://localhost:9411
- **Prometheus**: http://localhost:9090
- **Grafana**: http://localhost:3000 (admin/admin)
- **Jenkins**: http://localhost:8085

## 📊 Grafana

После запуска Grafana автоматически настраивается с:
- **Datasource**: Prometheus (преднастроен)
- **Dashboard**: Microservices Dashboard

Dashboard включает:
- HTTP Request Rate по сервисам
- HTTP Response Time
- Статус сервисов (UP/DOWN)
- JVM Memory Usage
- JVM Threads
- CPU Usage

## 🔨 Jenkins CI/CD

### Первоначальная настройка

1. Откройте http://localhost:8085
2. Войдите (admin/admin)
3. Создайте Pipeline job с использованием `Jenkinsfile`

### Pipeline включает:

1. **Checkout** - получение кода
2. **Build Contracts** - сборка контрактов
3. **Build Services** - параллельная сборка всех сервисов
4. **Run Tests** - запуск тестов
5. **Build Docker Images** - сборка Docker образов
6. **Deploy** - деплой в Docker
7. **Health Check** - проверка работоспособности

## 🛠️ Полезные команды

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f demo-rest

# Перезапуск сервиса
docker-compose restart demo-rest

# Пересборка конкретного сервиса
docker-compose build demo-rest
docker-compose up -d demo-rest

# Проверка статуса
docker-compose ps

# Полная очистка (включая volumes)
docker-compose down -v

# Проверка метрик сервиса
curl http://localhost:8080/actuator/prometheus

# Проверка здоровья сервиса
curl http://localhost:8080/actuator/health
```

## 📁 Структура проекта

```
adm/
├── docker-compose.yml      # Конфигурация Docker
├── prometheus.yml          # Конфигурация Prometheus
├── Jenkinsfile            # CI/CD Pipeline
├── build.bat              # Скрипт сборки
├── start.bat              # Скрипт запуска
├── stop.bat               # Скрипт остановки
├── prepare-libs.bat       # Подготовка библиотек
├── grafana/
│   ├── provisioning/
│   │   ├── datasources/   # Источники данных Grafana
│   │   └── dashboards/    # Конфигурация dashboards
│   └── dashboards/        # JSON файлы dashboards
├── jenkins/
│   └── casc.yaml          # Jenkins Configuration as Code
├── demo-rest/             # Основной REST API
├── analytics-service/     # gRPC сервис аналитики
├── audit-service/         # Сервис аудита
├── ws/                    # WebSocket сервис
├── events-contract/       # Контракты событий
└── books-api-contract/    # API контракты
```

## ⚠️ Troubleshooting

### Сервис не запускается
```bash
# Проверьте логи
docker-compose logs <service-name>

# Проверьте что все зависимости запущены
docker-compose ps
```

### Prometheus не видит метрики
- Проверьте что сервисы отвечают на `/actuator/prometheus`
- Проверьте сетевое подключение в Docker network

### Grafana dashboard пустой
- Подождите 1-2 минуты после запуска
- Проверьте что Prometheus datasource настроен
- Убедитесь что сервисы генерируют метрики

### Jenkins не запускается
- Убедитесь что Docker socket доступен
- Проверьте права доступа к volumes

## 📝 Примечания

- Все данные (RabbitMQ, Prometheus, Grafana, Jenkins) сохраняются в Docker volumes
- Для production окружения рекомендуется изменить пароли по умолчанию
- Multi-stage Dockerfile позволяет собирать образы без локальной установки Maven

